/** @type {import("prettier").Config} */
const config = {
  // ステートメントの末尾にセミコロンを出力する
  semi: true,
  // タブの幅
  tabWidth: 2,
  // タブではなくスペースを使用する
  useTabs: false,
  // シングルクォートの代わりに二重引用符を利用する
  singleQuote: false,
  // 1行の最大文字数
  printWidth: 100,
};

export default config;
